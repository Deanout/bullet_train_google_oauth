<%= render 'account/shared/page' do |page| %>
  <% page.title t('.section') %>
  <% page.body do %>
    <%= render 'account/shared/box' do |box| %>
      <% box.t :description, title: '.header' %>
      <% box.actions do %>
        <%= link_to t('.buttons.edit'), [:edit, :account, @google_account], class: first_button_primary if can? :edit, @google_account %>
        <%= button_to t('.buttons.destroy'), [:account, @google_account], method: :delete, class: first_button_primary, data: { confirm: t('.buttons.confirmations.destroy', model_locales(@google_account)) } if can? :destroy, @google_account %>
        <%= link_to t('global.buttons.back'), [:account, @user, :oauth_google_accounts], class: first_button_primary %>
      <% end %>
    <% end %>

    <%= render 'account/integrations/google_installations/list', context: @user, google_installations: @google_account.integrations_google_installations %>
    <%# 🚅 super scaffolding will insert new children above this line. %>

  <% end %>
<% end %>
